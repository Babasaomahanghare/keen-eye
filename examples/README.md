# Examples

This directory intentionally contains no real photos or intelligence data.

Use the offline demo from the repository root:

```bash
keeneye demo
```

For a real authorized image:

```bash
keeneye image /path/to/your/image.jpg --yes
```